import Image from "next/image";
import styles from "./page.module.css";
import Markdown from "react-markdown";

export default function Home() {
  return <main className="markdown">{/* <Markdown>{README}</Markdown> */}</main>;
}
